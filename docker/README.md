# docker/

Dockerfiles for `apps/web` and `apps/api` land here in Phase 15. Empty for
now — see `docker-compose.yml` at the repo root for the local dev stack that
exists today (PostgreSQL + Redis only).
